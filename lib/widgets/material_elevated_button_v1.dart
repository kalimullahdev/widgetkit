import 'package:flutter/material.dart';

class MaterialElevatedButtonV1 extends StatelessWidget {
  const MaterialElevatedButtonV1({super.key});

  @override
  Widget build(BuildContext context) {
    return ElevatedButton(
      onPressed: () {},
      child: const Text("Button v1"),
    );
  }
}
